# Owner Stripe (test first). See docs/OWNER_HANDOFF.md
STRIPE_SECRET_KEY=
# Shop webhook only — Stripe CLI locally, Dashboard endpoint in production
STRIPE_WEBHOOK_SECRET=
# Chimpsheets Auth user UUID for the owner login (not the demo test@email.com)
CHIMP_SHEETS_OWNER_ID=
# Production only (Neon). Local shop uses PGLite if unset.
# DATABASE_URL=
